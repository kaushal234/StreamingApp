apiVersion: v2
name: streamingapp
description: A Helm chart for the StreamingApp multi-service MERN application (auth, streaming, admin, chat, frontend + MongoDB)
type: application
version: 0.1.0
appVersion: "1.0.0"
